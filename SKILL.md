---
name: archive-catalog
description: 处理档案电子目录（Excel 归档文件目录/卷内文件目录/案卷目录）的批处理与校验。触发于「处理档案目录」「归档目录」「档号规范化」「合并总目录」「OCR修正」等场景。适用于各单位（如总工会、县委办、宣传部、统战部）的档案目录成品构建、批次并入与最终校验。
---

# 档案电子目录批处理

处理「档案/电子目录」项目下各单位档案目录：把 OCR 生成的源 Excel 解析、规范化、构建为统一格式成品，并支持分批并入总目录与最终校验。

## 适用场景

- 新单位/新批次的源文件 → 构建成品
- 新文件并入已确认的总目录（**必须基于已确认成品，勿从原始源重建**）
- 成品最终校验
- 源文件为 `.docx` 目录（县委办新格式）时需解析 Word 表格

## 核心概念

| 类型 | 表头 | 一行代表 | 输出 |
|------|------|---------|------|
| **案卷目录** | `案卷号\|题名\|年度\|页数\|期限\|备注` | 一卷 | 卷级 |
| **卷内文件目录** | 按卷分组，`序号\|文号\|责任者\|题名\|日期\|密级\|页数\|备注` | 一件 | 件级，无档号 |
| **归档文件目录** | `序号\|档号\|文号\|责任者\|题名\|日期\|密级\|页数\|备注` | 一件 | 件级，带档号 |

## 处理流程

### 1. 识别源文件结构

打开源文件，读取首个 Sheet，判断属于哪一类：

- **归档文件目录**：表头含「档号」，档号形如 `1031-WS ·2006-Y-001` → 件级带档号，按「保管期限」分段
- **卷内文件目录**：有「×××-Y-×××」卷号行作分节，各卷内表头+数据行 → 件级无档号，按年度分 Sheet
- **案卷目录**：表头为「案卷号|题名|…」→ 卷级，按原样保留

### 1.5 格式差异应对（面对陌生/不同格式）

**先探查再动手**：任何源文件先读进来看（多 Sheet、列偏移、表头位置、分段方式、有无档号、日期格式、`.xls`/`.docx`/`.zip`）。判据看**表头关键词**（档号/题名/案卷号），不看文件名。

| 变体 | 应对 |
|------|------|
| 列偏移（A 空 B 有数） | 字段映射从 B 起算 |
| 表头不在第 1 行 / 重复出现 | 扫描含「档号/题名/案卷号」行为表头，每次重建映射 |
| 按期限/卷号/节标题分段 | 记住当前段归属，跨段防污染 |
| 无档号 / 缺档号 | 档号列留空，**不编造** |
| 日期格式多样（`2018-1-1`/`2018年1月1日`/序列号） | 统一转 8 位 `YYYYMMDD` |
| `.xls` 老格式 | 先转 `.xlsx` 再读 |
| `.docx` 目录（县委办新格式） | `python-docx` 读表，决定 Sheet 与件数 |
| `.zip` 多文件 / 多 Sheet | 全部解析后合并 |

**决定自动改还是问用户**：固定 OCR 错字→自动修；格式差异→自动适配；缺项/缺号→留空；语义不确定→黄色高亮；涉及猜测（非法日期补位、疑重复）→**单独询问用户**。详见 `references/format-adaptation.md`。

### 2. 提取数据行

- 表头行识别：行内含「档号」或「题名」字样 → 记录字段名→列号映射（**注意列偏移**：A 空但 B 有数）
- 数据行识别：A 列 clean 后为纯数字序号（或 `·`/`-` 开头，去符号后补 3 位）
- **跳过**：标题行（"归档文件目录"/"档案文件目录"）、表头行、"保管期限"节标题行、全空行、OCR 残留行
- **删除规则**：只有序号、其余内容全空 → 删除；完整文件缺个别项 → 保留
- 核心内容（档号+责任者+题名）不含中文和数字 → 无内容删除
- 跨文件同卷续接（如同一档号分存两批）自动合并；同卷同序号重复保留先出现者
- 县委办新格式：`.docx` 目录表（单位|机构|保管期限|起止件号）→ 决定 Sheet 列表与件数分配

### 3. 规范化（每字段）

调用参考文件 `references/format.md` 中的规范，参照项目内 `_build_*.py` 脚本的 `norm_*` 函数实现：

- **档号** `norm_dh`：`全宗号-WS ·2006-Y-001` → `全宗号-ws·2006-y-0001`（如 `1031-ws·2006-y-0001`）。小写、`·`无空格、件号 4 位补零、期限码 `y`=永久 / `d30`=30年。修正 `ws-`/`ws。`/`ws,`/`ws:`/`1012-s`→`ws`、`200S`→`2006` 等 OCR 错。全宗号参数化（常见：总工会 1031 / 县委办 1001 / 统战部 1012）。
- **文号** `norm_wh`：方括号 `［］〔〕「」【】`→半角 `[]`；圆括号→全角 `（）`；去所有空白；OCR 修正
- **题名** `norm_tm`：去空格/开头残留标点；方括号→半角、圆括号→全角；书名号 `《》` 配对（开多于闭补闭号）；OCR 修正
- **责任者** `norm_zrz`：去空白；多机构分隔统一全角分号；OCR 修正
- **页数** `norm_pg`：数字补 3 位；区间 `001-004` 两端各补 3 位
- **日期** `norm_dt`：保留 8 位字符串
- **密级/备注**：去空格；OCR 残留（不含 ≥2 个不同汉字）清空

### 4. 日期异常分级处理

| 形态 | 例子 | 处理 |
|------|------|------|
| 8 位合法 | `20110300` / `20180927` | 保留（仅到年=`YYYY0000`、到月=`YYYYMM00` 合法） |
| **9 位占位（00月00日）** | `201000000` / `202000000` | **一律留空**并取消高亮 |
| 8 位月份非法 | `20188927`（89月） | 单独询问用户，通常按最可能日期改（如 `20180927`） |
| 7 位缺位 | `2019000` / `2019012` | 单独询问用户补位（如 `20190000` / `20190120`） |

### 5. 不确定项高亮

- 易识别错误 → 自动修正（见 `references/ocr-fixes.md`）
- **不确定项 → 黄色高亮 `FFFF00`**，供人工核对；**用户核对后取消高亮即视为确认**
- 高亮 key 用 `(年份, 期限码大写, 件号)` 定位，值是要高亮的字段名集合

### 6. 输出成品格式

统一格式（与县委办/统战部/总工会成品一致）：

```
行1 : 档案文件目录     （合并 A:I，宋体 16，居中）
行2 : 保管期限：永久   （合并 F:I，宋体 12，居中）
行3 : 序号|档 号|文号|责任者|题 名|日 期|密级|页数|备注 （宋体 12，全边框，居中，自动换行）
行4+: 数据行（宋体 12，全边框，居中，自动换行，行高按内容自动）
```

- 9 列（带档号）列宽：`A 5.255 B 9.755 C 11.755 D 15.255 E 22.873 F 9.627 G 5.127 H 5.127 I 5.127`
- 7 列（无档号）：`A:5.25 B:12 C:15 D:25 E:10 F:6 G:10`
- 序号年内连续 3 位 `001`；冻结首 3 行 `A4`
- 有档号 → 按「年度×期限」分 Sheet（`2006永久` / `2008-30年`），期限排序永久在前；无档号 → 按年度分 Sheet

### 7. 批次并入总目录

**关键：合并新批次必须基于用户已确认的成品，不得从原始源重新生成**（否则会重放用户已取消的高亮、丢失人工修正）。

> ⚠️ **实测教训**：同一批源文件「从原始源重建」 vs「基于确认成品并入」结果差异显著——
> 总工会 2166 件确认版 **0 高亮**；重建版却 **29 处高亮**（重放了已取消的）+ **6 处日期修正丢失**
> （`20188927`→`20180927`、`2019000`→`20190000` 等原始异常值全数回来）。
> 这些人工确认结果**只存在于成品**，源文件里没有，重建必然丢失。

```
备份成品 → python scripts/archive_cli.py merge 成品.xlsx 新文件.xlsx → 处理新批次异常日期/高亮 → python scripts/archive_cli.py check 成品.xlsx
```

> `scripts/archive_cli.py` 集成 build/merge/check/highlight 四个命令，全宗号在文件顶部 QUANZONG 修改。
> merge 的输入是**原始新批次源文件**（含「保管期限」分段、未规范档号），不是已规范化成品。

### 8. 最终校验清单

- Sheet 结构、件数统计（各 Sheet + 总计）
- 序号年内连续、无缺号、无重复
- 档号全部符合规范、**唯一无重复**
- 无残留黄色高亮
- 日期全部合法（8 位、月/日合法或 00）
- 页数为数字或 `ddd-ddd` 区间
- 与备份对比确认只改了预期内容

## 使用参考文件

- `references/format.md` — 详细输出格式、各字段规范、日期/高亮规则
- `references/format-adaptation.md` — 格式差异应对（面对陌生/不同格式的源文件如何识别与适配）
- `references/ocr-fixes.md` — OCR 修正清单（通用 + 各单位积累）
- `scripts/norm_common.py` — 通用字段规范化函数模板
- `examples/example.md` — 处理效果示例与典型 OCR 错例

## 已知规范速查

- 档号规范：`全宗号-ws·年度-y-件号` / `全宗号-ws·年度-d30-件号`，小写、`·` 分隔、件号 4 位
- 期限码：`y`=永久、`d30`=30年、`d10`=10年
- 全宗号按单位参数化（常见：总工会 1031 / 县委办 1001 / 统战部 1012）
- 多机构责任者间补空格（`市委统战部市民宗局`→`市委统战部 市民宗局`），但不拆 `办公室`/`委员会` 完整后缀
